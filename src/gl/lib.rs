Error: Required option 'type' missing.
generator

Options:
    --namespace gl|glx|wgl
                        OpenGL namespace (gl by default)
    --api gl|gles1|gles2
                        API to generate bindings for (gl by default)
    --type ptr|struct   Binding type to generate (ptr by default)
    --profile core|compatability
                        Profile to generate (compatability by default)
    --version           Version to generate bindings for (4.3 by default)
    --extension         Extension to include

