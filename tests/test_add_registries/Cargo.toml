[package]
name = "test_add_registries"
version = "0.0.0"
build = "build.rs"
publish = false
edition = "2018"

[lib]
path = "lib.rs"

[build-dependencies]
gl_generator = { path = "../../gl_generator" }
